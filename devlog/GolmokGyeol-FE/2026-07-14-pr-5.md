# 2026-07-14

## 요약
본 Pull Request는 `GolmokGyeol-FE` 프로젝트의 전반적인 코드 리팩토링과 불필요한 소스 코드 정리 작업을 수행했습니다. 사용하지 않는 컴포넌트와 커스텀 훅을 제거하고 스타일링 방식을 Emotion으로 전환하여 전역 스타일 파일의 크기를 대폭 줄였습니다. 또한 패키지 관리 도구를 정리하고 후기가 없는 상가 데이터를 수정하여 시스템의 일관성을 높였습니다.

## 변경 내용
- **미사용 코드 및 파일 제거**: 사용하지 않는 컴포넌트인 `SearchForm.tsx`와 커스텀 훅 `useTasteDrag.ts`를 완전히 삭제했습니다.
- **스타일링 방식 개선**: `globalStyles.ts` 파일에서 대규모 스타일 코드를 제거하고, 개별 컴포넌트들의 스타일 설정을 Emotion 기반으로 수정했습니다.
- **패키지 관리 도구 정리**: `yarn.lock` 파일을 삭제하고 `package-lock.json` 및 `package.json`을 업데이트하여 의존성 관리 도구를 정비했습니다.
- **데이터 구조 개선**: 후기가 존재하지 않는 상가 데이터를 삭제하고 관련 로직을 수정했습니다 (`restaurants.ts` 및 관련 타입 변경).
- **컴포넌트 리팩토링**: `TasteMap`, `RestaurantSidebar`, `NeighborhoodSelector` 등 지도 및 랜딩 페이지 내 주요 컴포넌트들의 코드를 재구성했습니다.

## 의도
프로젝트 내에 누적된 미사용 파일과 레거시 스타일 코드를 정리하여 코드베이스의 가독성과 유지보수성을 향상시키고자 했습니다. 또한, 일관되지 않던 패키지 잠금 파일을 하나로 정리하여 협업 시 발생할 수 있는 의존성 충돌을 방지하고, 실제 비즈니스 로직에 맞지 않는 예외 데이터(후기 없는 상가)를 미리 정제하려는 의도로 파악됩니다.

## 포함된 커밋
- `c8ed316` fix: 후기 없는 상가 삭제
- `99acebf` refactor: 안쓰는 코드 정리
- `aab3b06` fix: style emotion으로 수정

## 변경 파일
- `yarn.lock` (삭제), `package-lock.json` (수정): 패키지 관리 방식을 정리하여 의존성 잠금 파일을 일원화했습니다.
- `src/app/globalStyles.ts`: 전역 스타일 중 불필요한 부분을 대거 삭제(-738 라인)했습니다.
- `src/components/landing/SearchForm.tsx`, `src/hooks/useTasteDrag.ts` (삭제): 더 이상 사용되지 않는 컴포넌트와 훅을 제거했습니다.
- `src/components/taste-map/TasteMap.tsx`, `RestaurantSidebar.tsx`: 지도 및 사이드바 내 스타일 지정 방식과 렌더링 로직을 개선했습니다.
- `src/mocks/restaurants.ts`: 후기가 없는 상가 데이터를 테스트 데이터 셋에서 제외했습니다.

## 생각 정리
1. `yarn.lock`을 삭제하고 `package-lock.json`만 남겨둔 결정은 팀 내 패키지 관리 도구를 npm으로 완전히 통일하기 위함이었나요?
2. `globalStyles.ts`에 정의되어 있던 스타일들을 각 컴포넌트 내부에서 Emotion으로 다듬으면서 컴포넌트의 독립성이 얼마나 향상되었나요?
3. 대규모 리팩토링을 통해 불필요한 코드를 1,000줄 이상 덜어내면서 개발자로서 느낀 코드 다이어트의 효용과 깨달음은 무엇인가요?

## PR
- 프로젝트: GolmokGyeol-FE
- 저장소: 2026-DSM-8/GolmokGyeol-FE
- 번호: #5
- 상태: merged
- 브랜치: develop -> main
- 변경량: +1700/-2723, 파일 24개
- 시간: 2026-07-13T16:20:15Z
- 링크: https://github.com/2026-DSM-8/GolmokGyeol-FE/pull/5
