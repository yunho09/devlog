# 2026-09-19

## 요약
업무 보고 상세 페이지에서 이미 심사(승인 또는 반려) 완료된 보고서의 경우 승인 및 반려 버튼이 화면에 보이지 않도록 개선한 PR입니다. 이미 처리된 업무 보고에 대한 중복 심사 시도를 방지하여 관리자 페이지의 오작동을 예방합니다. 관련된 E2E 테스트 코드도 변경된 사양에 맞추어 보완하였습니다.

## 변경 내용
- **버튼 노출 조건부 렌더링 구현**: `APPROVED` 또는 `REJECTED` 상태인 업무 보고서 상세 화면에서 승인 및 반려 처리를 수행하는 버튼들이 표시되지 않도록 수정했습니다.
- **E2E 테스트 코드 보완**: 이미 심사 완료된 업무 보고에 대해 버튼이 실제로 숨겨져 있는지 검증하는 테스트 코드를 추가하거나 수정하였습니다.

## 의도
이미 처리가 완료된 업무 보고서에 대해 관리자가 다시 승인이나 반려 처리를 시도하지 못하도록 제한하기 위함입니다. 이를 통해 데이터의 정합성을 유지하고, 불필요한 UI 요소를 제거하여 관리자의 혼선을 줄이고자 했습니다.

## 포함된 커밋
- 221097b fix: 이미 심사된 업무보고 승인·반려 버튼 숨김

## 변경 파일
- `src/pages/task-reports/TaskReportDetailPage.tsx`: 업무 보고서 상세 페이지 파일로, 보고서의 심사 상태에 따라 승인/반려 버튼의 렌더링 여부를 결정하도록 로직을 수정했습니다.
- `tests/e2e/task-report.spec.ts`: 종단 간(E2E) 테스트 코드로, 심사 완료된 보고서에서 승인 및 반려 버튼이 보이지 않는지 확인하는 검증 로직이 추가되었습니다.

## 트러블 슈팅
- PR 정보 기준으로는 기록된 트러블 슈팅이 없습니다.

## 생각 정리
1. `TaskReportDetailPage`에서 업무 보고서의 심사 상태(`APPROVED`, `REJECTED` 등)를 어떤 상태 데이터나 API 응답 필드를 기준으로 판별하고 있는가?
2. `task-report.spec.ts` 테스트 코드에서 심사 완료 상태의 화면을 재현하기 위해 데이터를 어떻게 모킹(Mocking)하거나 세팅하였는가?
3. 이미 처리된 항목을 아예 숨기는 방식과, 비활성화(Disabled) 처리하여 이전의 심사 이력을 시각적으로 남겨두는 방식 중 어떠한 UX가 관리자에게 더 친절한 피드백을 제공할까?
