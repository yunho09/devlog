# 2026-09-02

## 요약
Commonly-Frontend 프로젝트에서 PR #67 "feat: 미연동 API 연동 (초기 비밀번호 변경, 경력 수정 직종/부서, 업무이력 필터, 통합등록 결과) (#66)" 작업이 기록되었습니다. AI API를 사용할 수 없어 PR 정보 기준의 기본 노트로 생성했습니다.

## 변경 내용
- PR 제목: feat: 미연동 API 연동 (초기 비밀번호 변경, 경력 수정 직종/부서, 업무이력 필터, 통합등록 결과) (#66)
- PR 본문: Closes #66

## 요약

백엔드(`Commonly-Backend` develop) 기준으로 프론트에서 아직 연동되지 않았거나, 백엔드가 이미 지원하는데 화면이 쓰지 않던 부분을 연동했다. 백엔드 변경 없이 프론트만으로 처리 가능한 항목만 포함.

## 변경 사항

- **초기 비밀번호 변경** (`PATCH /api/admin/password`) — admin-web/user-web 에 `/password/initial` 화면 추가. 직원 계정은 초기 비밀번호를 바꾸기 전까지 모든 API 가 403 이므로, 로그인 직후 프로브로 감지해 변경 화면으로 보내고, 이후 어떤 API 에서든 해당 403 이 오면 `PASSWORD_CHANGE_REQUIRED_EVENT` 로 변경 화면으로 안내. 변경 후 원래 화면으로 복귀.
- **경력 수정** — 경력 목록 응답의 `jobTitle`/`department` 를 화면에 채움 (기존엔 직위가 항상 빈 값이라 저장 시 덮어썼음). 수정 요청에 `department` 전송, `division`(구분) 은 원본 유지 (백엔드가 채용/전보/해지/퇴직만 허용).
- **업무이력 조회** — 백엔드가 지원하는 `startDate`/`endDate`/`keyword` 필터 UI (URL 쿼리 동기화). 백엔드 실제 응답(배열, `issuedAt`/`purpose`/`documentNo`) 호환, 전체 페이지 수가 없을 때 `hasNextPage` 기반 페이징.
- **사용자 목록** — 백엔드 실제 응답(배열) 호환 및 동일한 페이징 처리.
- **통합 등록** — 완료 화면에 `failedRows`(행 번호·사유) 목록 표시, 미리보기에서 행 이동으로 전체 행 확인, 매핑 필드를 `CERTIFICATE_TARGET_FIELDS` 단일 소스로 통일, 컴포넌트 더미 기본값(홍길동/00건/1행…) 제거.
- **개별 등록** — 대상자 확정 단계 연동. 중복 확인에서 기존 대상자를 고르면 그 `humanId` 를 쓰고(주소를 새로 입력한 경우에만 `PUT /api/human/{id}` 로 갱신), 아니면 `POST /api/human` 으로 새 대상자를 만들며 **주소지·근무부서를 함께 전송**. 대상자 id 를 기억해 경력 등록만 실패한 뒤 재시도해도 중복 생성되지 않음. 경력 행 등록(`POST /api/certificates/create`)은 백엔드 미구현이라 여전히 실패하지만, 실패 메시지에 "대상자 등록은 완료" 를 구분해 표시.
- **헤더** — 세션 연장 API 는 없지만 프로토타입 UI 유지를 위해 "연장" 버튼은 항상 표시 (동작 없음).
- **빌드 수정** — 루트 `vite.env.ts` 가 CJS 로 인식되어 세 앱 모두 `tsc -b` 가 실패하던 문제 (루트 `package.json` 에 `"type": "module"`, `vite` devDependency 추가). `9426226` 이후 develop 에서 `bun run build` 가 깨져 있었음 — Dockerfile 도 같은 명령을 쓰므로 배포에도 영향.
- **테스트** — `fcaa65d` 에서 더미 기본값을 제거한 뒤 깨져 있던 `UserList`/`IndividualRegistrationComplete` 테스트 수정.

## 검증

- `packages/utils` bun test 118 pass, tsc/oxlint clean
- `packages/ui` bun test 16 pass, tsc/oxlint clean
- admin-web / user-web / civil-web `bun run build` 성공

## 이번 범위에서 제외 (백엔드 선행 필요)

- 경력사항 개별 등록의 **경력 행 등록**: `POST /api/certificates/create` 미구현 (대상자 생성·주소·기존 대상자 선택은 이번에 `POST/PUT /api/human` 으로 연동함)
- 발급 사유 / 수정 사유: 요청 DTO 에 필드 없음
- 세션 연장, 아이디/비밀번호 찾기: API 없음
- 민원인 본인인증(provider 미정), 본인 경력 조회 `GET /api/certificates/self` 미구현, 본인 발급 `self-issue-enabled=false`
- 증명서 미리보기 서버 렌더(발급기관/발급일/문서번호)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01SAoL3QSr5ox2JwVczYCVPL


<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **새로운 기능**
  * 최초 로그인 시 초기 비밀번호 변경 화면을 제공하며, 변경 후 원래 화면으로 돌아갈 수 있습니다.
  * 업무 이력에서 기간과 대상자명으로 검색하고 조건을 초기화할 수 있습니다.
  * 대량 등록 미리보기에서 행별로 이동하고, 등록 실패 행과 사유를 확인할 수 있습니다.
  * 개별 등록 시 대상자를 새로 등록하거나 기존 대상자를 재사용할 수 있습니다.
* **개선 사항**
  * 사용자 및 발급 이력 목록에서 전체 페이지 수가 없어도 다음 페이지를 탐색할 수 있습니다.
  * 경력증명서에 부서 정보와 직무명이 더 정확하게 표시됩니다.
  * 세션 연장 버튼이 항상 표시됩니다.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->

- 변경 파일: 45개 (+1955/-183)

## 의도
PR 정보 기준으로는 해당 변경이 프로젝트 작업 내역을 기록하기 위해 추가되었습니다.

## 포함된 커밋
- e773477 feat: 초기 비밀번호 변경 API 클라이언트 추가 및 목록 응답 배열 형태·경력 직종/부서 필드 호환
- f121c4f fix: 루트 vite.env.ts 를 ESM 으로 인식시키고 vite 타입을 찾도록 해 앱 빌드(tsc -b) 오류 수정
- c97cdb8 test: 더미 기본값 제거 이후 깨진 UserList·IndividualRegistrationComplete 테스트 수정
- 5053f18 feat: 초기 비밀번호 변경 화면 추가 및 로그인 직후·403 응답 시 변경 화면으로 안내
- b534fe9 feat: 업무이력 조회 기간·성명 필터 연동 및 목록 페이지 수 미제공 시 다음 페이지 기반 페이징
- 2bce62d feat: 경력 수정에 직종명·근무부서 연동, 세션 연장 API 없는 헤더 연장 버튼 숨김
- 6589243 feat: 통합 등록 실패 행 상세 표시·미리보기 행 이동 추가, 매핑 필드를 CERTIFICATE_TARGET_FIELDS 로 통일
- 7087a1f revert: 프로토타입 UI 유지를 위해 헤더 세션 연장 버튼 항상 표시로 복구
- c1da1d9 feat: 대상자 등록 API(POST /api/human) 클라이언트 createHuman 추가
- afc6f78 feat: 개별 등록 시 대상자 생성·주소지 전송·기존 대상자 선택 연동 후 humanId 로 경력 등록
- 3d5c5ae fix: 기존 대상자 부서 갱신을 주소 입력 여부와 분리하고 미입력 시 기존 주소 유지
- eea2a7c fix: 사용자 목록 다음 페이지 판단을 원본 응답 행 수 기준으로 변경

## 변경 파일
- apps/admin-web/src/layout/AdminLayout.tsx (modified, +15/-0)
- apps/admin-web/src/pages/IndividualRegistrationCareerPage.tsx (modified, +122/-5)
- apps/admin-web/src/pages/InitialPasswordChangePage.tsx (added, +2/-0)
- apps/admin-web/src/pages/IntegratedRegistrationCompletePage.tsx (modified, +1/-0)
- apps/admin-web/src/pages/IntegratedRegistrationPreviewPage.tsx (modified, +8/-2)
- apps/admin-web/src/pages/UserListPage.tsx (modified, +14/-9)
- apps/admin-web/src/pages/WorkHistoryPage.tsx (modified, +131/-13)
- apps/admin-web/src/router/index.tsx (modified, +5/-0)
- apps/user-web/src/layout/UserLayout.tsx (modified, +15/-0)
- apps/user-web/src/pages/IndividualRegistrationCareerPage.tsx (modified, +122/-5)
- apps/user-web/src/pages/InitialPasswordChangePage.tsx (added, +2/-0)
- apps/user-web/src/pages/IntegratedRegistrationCompletePage.tsx (modified, +1/-0)
- apps/user-web/src/pages/IntegratedRegistrationPreviewPage.tsx (modified, +8/-2)
- apps/user-web/src/router/index.tsx (modified, +5/-0)
- bun.lock (modified, +1/-0)
- package.json (modified, +3/-1)
- packages/ui/src/header/Header.tsx (modified, +5/-8)
- packages/ui/src/hooks/usePasswordChangeGuard.ts (added, +23/-0)
- packages/ui/src/index.ts (modified, +6/-0)
- packages/ui/src/pages/InitialPasswordChangePage.tsx (added, +198/-0)
- packages/ui/src/pages/LoginPage.tsx (modified, +14/-1)
- packages/ui/src/pages/StaffCareerEditPage.tsx (modified, +7/-5)
- packages/ui/src/registration/integrated-registration-complete/IntegratedRegistrationComplete.tsx (modified, +52/-8)
- packages/ui/src/registration/integrated-registration-complete/integratedRegistrationComplete.styles.ts (modified, +81/-0)
- packages/ui/src/registration/integrated-registration-confirm/IntegratedRegistrationConfirm.tsx (modified, +21/-34)
- packages/ui/src/registration/integrated-registration-preview/IntegratedRegistrationPreview.tsx (modified, +62/-31)
- packages/ui/src/registration/integrated-registration-preview/integratedRegistrationPreview.styles.ts (modified, +45/-0)
- packages/ui/src/work-history/WorkHistory.styles.ts (modified, +46/-0)
- packages/ui/src/work-history/WorkHistory.tsx (modified, +166/-2)
- packages/ui/src/work-history/workHistoryFilters.ts (added, +60/-0)
- packages/ui/tests/IndividualRegistrationComplete.test.tsx (modified, +4/-3)
- packages/ui/tests/UserList.test.tsx (modified, +4/-4)
- packages/ui/tests/WorkHistory.test.tsx (modified, +96/-1)
- packages/utils/src/__tests__/admins.test.ts (modified, +41/-3)
- packages/utils/src/__tests__/certificates.test.ts (modified, +7/-0)
- packages/utils/src/__tests__/humans.test.ts (modified, +66/-0)
- packages/utils/src/__tests__/issuanceHistories.test.ts (modified, +41/-3)
- packages/utils/src/__tests__/password.test.ts (added, +130/-0)
- packages/utils/src/admins.ts (modified, +36/-18)
- packages/utils/src/api.ts (modified, +70/-3)
- packages/utils/src/certificates.ts (modified, +15/-1)
- packages/utils/src/humans.ts (modified, +50/-1)
- packages/utils/src/index.ts (modified, +26/-0)
- packages/utils/src/issuanceHistories.ts (modified, +54/-20)
- packages/utils/src/password.ts (added, +74/-0)

## 트러블 슈팅
- fix: 루트 vite.env.ts 를 ESM 으로 인식시키고 vite 타입을 찾도록 해 앱 빌드(tsc -b) 오류 수정
- test: 더미 기본값 제거 이후 깨진 UserList·IndividualRegistrationComplete 테스트 수정
- feat: 경력 수정에 직종명·근무부서 연동, 세션 연장 API 없는 헤더 연장 버튼 숨김
- revert: 프로토타입 UI 유지를 위해 헤더 세션 연장 버튼 항상 표시로 복구
- fix: 기존 대상자 부서 갱신을 주소 입력 여부와 분리하고 미입력 시 기존 주소 유지
- fix: 사용자 목록 다음 페이지 판단을 원본 응답 행 수 기준으로 변경

## 생각 정리
- "feat: 미연동 API 연동 (초기 비밀번호 변경, 경력 수정 직종/부서, 업무이력 필터, 통합등록 결과) (#66)" 작업에서 가장 신경 써서 확인해야 했던 부분은 무엇이었나?
- apps/admin-web/src/layout/AdminLayout.tsx 변경이 전체 흐름에 어떤 영향을 준다고 봤나?
- 이 PR을 진행하면서 느낀 점이나 배운 점은 무엇이었나?

## PR
- 프로젝트: Commonly-Frontend
- 저장소: DSM2026-Commonly/Commonly-Frontend
- 번호: #67
- 상태: merged
- 브랜치: feature/66-api-integration -> develop
- 시간: 2026-09-02T14:09:15Z
- 링크: https://github.com/DSM2026-Commonly/Commonly-Frontend/pull/67
