# 2026-09-08

## 요약
도로명주소 검색 API 승인키를 발급받고 프로젝트에 설정하는 방법을 상세히 문서화하였습니다. 로컬 개발 환경 및 Portainer 배포 환경 각각에 필요한 환경 변수 설정법과 예시를 추가했습니다. 소스 코드 수정 없이 오직 개발 가이드 문서 보강만을 진행한 작업입니다.

## 변경 내용
- `deploy/PORTAINER.md`: '도로명주소 검색 API 승인키' 가이드를 추가하여 API 신청 경로, 입력 항목, 개발용/운영용 키의 차이, 환경 변수(`VITE_JUSO_CONFM_KEY`) 기입 위치를 명시했습니다.
- `README.md`: '로컬 환경 변수' 절을 추가하여 루트 디렉터리의 `.env.local` 작성 예시를 제공했습니다.
- `VITE_*` 환경 변수가 빌드 타임에 인라인되므로 값 변경 시 앱 재빌드가 필요하다는 주의 사항을 기재했습니다.

## 의도
도로명주소 API 사용에 필수적인 승인키를 발급하고 설정하는 과정을 공유하여, 동료 개발자들이 개발 환경을 구축하거나 서버를 배포할 때 겪을 수 있는 혼선을 방지하기 위함입니다. 특히 빌드 타임에 주입되는 환경 변수의 특성을 명시하여 빌드 오류나 런타임 누락을 사전에 예방하고자 했습니다.

## 포함된 커밋
- `48b9ec2` docs: 도로명주소 검색 API 승인키 발급 절차와 로컬 환경 변수 설정 정리

## 변경 파일
- `README.md`: 로컬 환경 변수 구성 예시 및 Vite 환경 변수의 빌드 시점 주입 특성을 명시하여 로컬 개발 환경 세팅에 영향을 줍니다.
- `deploy/PORTAINER.md`: Portainer를 통한 실제 배포 환경에서 도로명주소 API 승인키를 발급받고 적용하는 절차를 명시하여 운영 배포 설정에 영향을 줍니다.

## 트러블 슈팅
- PR 정보 기준으로는 기록된 트러블 슈팅이 없습니다.

## 생각 정리
1. `civil-web` 앱처럼 주소 검색 UI가 없는 경우, 빌드 과정에서 트리쉐이킹을 통해 환경 변수가 인라인 대상에서 자동 제외되는 현상을 명시하는 것이 협업 관점에서 왜 중요할까요?
2. 개발용 승인키(접두사 `dev`)와 배포 도메인이 등록되어야 하는 운영용 승인키의 물리적 차이를 명확하게 분리하여 가이드 문서를 작성할 때 가장 주의해야 할 점은 무엇일까요?
3. 이번 문서화 작업을 진행하면서, 복잡한 API 키 발급 및 빌드 타임 주입 과정을 구조적으로 기록해 두는 활동이 팀의 온보딩과 개발 생산성에 어떤 가치를 준다고 느끼셨나요?

## PR
- 프로젝트: Commonly-Frontend
- 저장소: DSM2026-Commonly/Commonly-Frontend
- 번호: #73
- 상태: merged
- 브랜치: feature/72-juso-key-docs -> develop
- 변경량: +45/-1, 파일 2개
- 시간: 2026-09-08T10:04:37Z
- 링크: https://github.com/DSM2026-Commonly/Commonly-Frontend/pull/73
