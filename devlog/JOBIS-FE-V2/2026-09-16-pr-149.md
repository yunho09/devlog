# 2026-09-16

## 요약
JOBIS-FE-V2 프로젝트에서 PR #149 "#146/api hooks" 작업이 기록되었습니다. AI API를 사용할 수 없어 PR 정보 기준의 기본 노트로 생성했습니다.

## 변경 내용
- PR 제목: #146/api hooks
- PR 본문: ## 작업 내용 설명
- #147 을 쪼갠 **스택 PR 2/7** 입니다. base 는 `#146/ds-components` (#148).
- `packages/api` 변경만 모았습니다. 뒤따르는 admin/student PR 이 이 훅에 의존합니다.

## 주요 변경 사항
- `createIdMutationHook` 의 URL 슬래시 중복(`/notices//1`) 수정
- params 가 없는 경로 함수 쿼리는 실행되지 않도록 차단
- 버그 제보 등록 훅(`bug-reports`) 추가
- 공지 목록 `page` 파라미터 지원 및 캐시 무효화 키 정정
- 취업 현황 조회 `year` 경로 반영, 취업 취소 훅 이름 명확화
- 모집전 상태를 `REQUESTED` 로 변경

## 결과물
API 훅만 추가해서 화면 변화가 없어, 훅을 사용하는 화면을 첨부합니다.

**공지 목록 (`useNoticeList`)**
![공지 목록](https://github.com/user-attachments/assets/fb762903-cc96-4d3e-9a4b-6c217d030c65)

**버그 제보 (`useCreateBugReport`)**
![버그 제보](https://github.com/user-attachments/assets/49a06bba-0a5c-410b-aec1-190a839346c3)

> 공지 목록은 가짜 API 응답(목 데이터)으로 캡처했습니다.

## 체크리스트
- [x] 빌드 되나요? (`yarn build`)
- [x] 콘솔에 에러 없나요? (`yarn dev`)
- [x] 목표한 부분만 수정했나요? (사이드 이펙트 체크)

## 해결 이슈
- #146

---

### 스택 순서
1. #148 `#146/ds components`
2. #149 `#146/api hooks`
3. #150 `#146/admin notice`
4. #151 `#146/admin student`
5. #152 `#146/st recruitment filter`
6. #153 `#146/st home banner design`
7. #154 `#146/st bug report`

위에서부터 순서대로 머지해 주세요. 전부 머지되면 #147 과 내용이 동일해집니다.

- 변경 파일: 11개 (+57/-14)

## 의도
PR 정보 기준으로는 해당 변경이 프로젝트 작업 내역을 기록하기 위해 추가되었습니다.

## 포함된 커밋
- 7021904 🐛 (AP) :: id 뮤테이션 URL 슬래시 중복 수정
- 3b1a766 🐛 (AP) :: 모집전 상태 REQUESTED로 변경
- afab7ad ♻️ (AP) :: 취업 취소 훅 이름 명확화
- e67013b ✨ (AP) :: 버그 제보 등록 훅 추가
- deed435 🐛 (AP) :: 취업 현황 조회 year 경로 반영
- 8501753 ✨ (AP) :: 공지 목록 page 파라미터 지원
- 8c4806e 🐛 (AP) :: params 없는 경로 함수 쿼리 실행 차단
- b75275b 🐛 (AP) :: 공지 목록 캐시 무효화 키 정정
- 0828e59 🐛 (AP) :: 버그 제보 등록 경로를 /bugs로 수정
- 9f17282 ♻️ (AP) :: 공지 목록 쿼리에서 미사용 page 파라미터 제거
- 801af06 🐛 (AP) :: 4xx 응답은 재시도하지 않도록 변경

## 변경 파일
- packages/api/src/acceptances/index.ts (modified, +4/-3)
- packages/api/src/acceptances/types.ts (modified, +1/-1)
- packages/api/src/applications/index.ts (modified, +4/-4)
- packages/api/src/applications/keys.ts (modified, +2/-2)
- packages/api/src/bug-reports/index.ts (added, +13/-0)
- packages/api/src/bug-reports/types.ts (added, +8/-0)
- packages/api/src/create-hook.ts (modified, +11/-2)
- packages/api/src/enum.ts (modified, +1/-1)
- packages/api/src/index.ts (modified, +1/-0)
- packages/api/src/notices/index.ts (modified, +1/-0)
- packages/api/src/query-client.ts (modified, +11/-1)

## 트러블 슈팅
- 🐛 (AP) :: id 뮤테이션 URL 슬래시 중복 수정
- ✨ (AP) :: 버그 제보 등록 훅 추가
- 🐛 (AP) :: 버그 제보 등록 경로를 /bugs로 수정

## 생각 정리
- "#146/api hooks" 작업에서 가장 신경 써서 확인해야 했던 부분은 무엇이었나?
- packages/api/src/acceptances/index.ts 변경이 전체 흐름에 어떤 영향을 준다고 봤나?
- 이 PR을 진행하면서 느낀 점이나 배운 점은 무엇이었나?

## PR
- 프로젝트: JOBIS-FE-V2
- 저장소: Team-return/JOBIS-FE-V2
- 번호: #149
- 상태: merged
- 브랜치: #146/api-hooks -> #146/ds-components
- 시간: 2026-09-16T12:50:19Z
- 링크: https://github.com/Team-return/JOBIS-FE-V2/pull/149
