# 2026-09-16

## 요약
JOBIS-FE-V2 프로젝트에서 PR #156 "#146/st api e2e" 작업이 기록되었습니다. AI API를 사용할 수 없어 PR 정보 기준의 기본 노트로 생성했습니다.

## 변경 내용
- PR 제목: #146/st api e2e
- PR 본문: ## 작업 내용 설명
- 로그인한 학생 계정으로 학생 앱 주요 페이지를 돌면서 API 에러를 잡는 Playwright e2e 테스트를 추가했습니다.
- 루트 `.env` 에 `E2E_STUDENT_ID`, `E2E_STUDENT_PASSWORD` 를 넣고 `yarn test:e2e` 로 실행합니다. 계정이 없으면 전부 skip 됩니다.
- 스택 PR #155 위에 올린 PR입니다. base 는 `#146/st-header-user-name` (#155).

## 주요 변경 사항
- `apps/student/e2e/api-errors.spec.ts`: 목록·마이페이지·상세 13개 페이지를 방문하고, 스테이징 API 4xx/5xx·요청 실패·페이지 에러·`/login` 리다이렉트가 있으면 실패 (`ERR_ABORTED` 는 제외). 상세 페이지 id 는 목록 API 첫 항목에서 가져오고, Sentry 전송은 차단합니다.
- `apps/student/playwright.config.ts`: 루트 `.env` 를 읽고 3000 포트에 student dev 서버를 띄웁니다. 스테이징 CORS 허용 origin 이 `localhost:5173/5174/3000` 이라 vite 기본 포트와 겹치지 않는 3000 을 씁니다.
- dev 서버는 `yarn run -T vite` 로 루트 vite 를 띄웁니다. student 워크스페이스에는 vite 가 없어 `yarn dev` 로 띄우면 전역 vite 가 잡힐 수 있습니다.
- 루트 `yarn test:e2e` 스크립트, `.env.example` 에 `E2E_STUDENT_ID`/`E2E_STUDENT_PASSWORD`, `.gitignore` 에 테스트 결과물 추가
- 새 의존성 없음 (기존 `playwright` 패키지의 `playwright/test` 사용)

## 결과물
![e2e 리포트](https://github.com/user-attachments/assets/b2125382-474a-4b31-b720-9ced7f405a08)

- `/` 의 `GET /interviews/students → 403` 은 백엔드에 없는 API 라 실제 문제로 남겨두고 예외 처리하지 않았습니다.

## 체크리스트
- [x] 빌드 되나요? (`yarn build`)
- [x] 콘솔에 에러 없나요? (`yarn dev`)
- [x] 목표한 부분만 수정했나요? (사이드 이펙트 체크)

## 해결 이슈
- resolved #146

- 변경 파일: 5개 (+177/-1)

## 의도
PR 정보 기준으로는 해당 변경이 프로젝트 작업 내역을 기록하기 위해 추가되었습니다.

## 포함된 커밋
- 18b19e5 🧪 (ST) :: 학생 앱 API 에러 e2e 테스트 추가
- 6fb20d2 🧱 (RT) :: e2e 테스트 스크립트와 환경 변수 예시 추가
- bd91763 🐛 (ST) :: e2e dev 서버를 루트 vite로 실행
- 4226d5f ♻️ (ST) :: e2e 로그인을 한 번만 수행하도록 변경

## 변경 파일
- .env.example (modified, +3/-1)
- .gitignore (modified, +4/-0)
- apps/student/e2e/api-errors.spec.ts (added, +139/-0)
- apps/student/playwright.config.ts (added, +30/-0)
- package.json (modified, +1/-0)

## 트러블 슈팅
- 🧪 (ST) :: 학생 앱 API 에러 e2e 테스트 추가

## 생각 정리
- "#146/st api e2e" 작업에서 가장 신경 써서 확인해야 했던 부분은 무엇이었나?
- .env.example 변경이 전체 흐름에 어떤 영향을 준다고 봤나?
- 이 PR을 진행하면서 느낀 점이나 배운 점은 무엇이었나?

## PR
- 프로젝트: JOBIS-FE-V2
- 저장소: Team-return/JOBIS-FE-V2
- 번호: #156
- 상태: merged
- 브랜치: #146/st-api-e2e -> #146/st-header-user-name
- 시간: 2026-09-16T12:52:21Z
- 링크: https://github.com/Team-return/JOBIS-FE-V2/pull/156
